JS: Sempre colocar ele no final da pagina para o site carregar ele depois e da tempo para o resto carregar antes
para usar no node e so digitar node "index.js", o js ele fuciona muito mais para clientes para trazer interacoes com
o cliente, ele tambem pode ser usado para servidores, primeira versao dele ECMAscrpit ele pulou a versao 4

TECNOLOGIAS: Voce precisa saber JavaScrpit para usar, jquery, angular, react, vue, electron, e tals
cliente x servido: cliente sao os dispositivos que os usuarios utilizam, para acessar o servido precisamos de um
intermediario no caso navegador

TAG: window.alert() faz aparecer um alerta, window.confirm() faz aparece uma janela de confirmacao, window.prompton() faz aperecer
uma janela para digitar coisas,

COMENTARIOS: Para fazer comentarios se utiliza // ou /* */, e muito importante para enetender o codigo

VARIAVEIS E TIPOS PRIMITIVOS: Sinal = significa recebe e nao pode significar igual porque ele pode receber outos valores
OQUE E UMA VARIAVEL? Ela armazenar dados que serao utilizados
VAR = Uma variavel para criat var nome/identificadores = recebe, nao pode usar mumeros no inico, e possivel usar assentos e simbolos,
 nao pode ter espacos e nao pode ter palavras reservadas "fuction,activ etc", para dar outro valor a variavel e so repetir ela so que
 sem o var de inicio
 var nome = "Biel"
 nome = "biel"
 TIPOS DE DADOS: number sao numeros, strings sao cadeias de caracteres e tambem pode ter numeros tipo cpf, telefone tals,
 boolean sao valores true e false, para ver qual e o tipo se utiliza typeof variavel
 DIFERENCA ENTRE `` E "" OU '': O `` a variavel e nsao so ela pode ser processada dentro do texto com ${} ja a '' ou "" tudo e tratado
 como um texto

MANIPULACAO DE DADOS: a variavel vai receber o valor e assim conseguir amazenar ele dentro dele, exemplo
var nome = windows.prompt(seu nome), o prompt vai pegar oque voce digitou e colocar dentro da variavel
VIRAR NUMERO UMA STRING: Se utiliza ou Number.parseInt(numero) ele coneverte o numero sem virgula 10
ou Number.parseFloat(numero) que ja converte com virgula ou so Number()
VIRAR NUMERO UMA STRING: String(numero ou letra) Faz vira uma letra
.length: Faz contar quantas strings tem minha palavra
.toUpperCase(): todas as letra fica maiuscula
.toLowerCase(): todas as letras fica minusculas
toFixed(2): Faz criar uma casa nova para os numeros 200.0 com ele 200.00
replace(".",","): Faz trocar um elemento por outro, eles sao trocados dentro das aspas exemplo 200.0 com ele 200,0

OPERADORES JS: aritimeticos, atribuicao, relacionais, logicos, ternarios
ARITIMETICOS: + adicao, - subtracao, * multiplicacao, / divisao com virgula/inteira, % resto da divisao inteira que sobrou
** potencia
ORDEM DE PRECEDENCIA: Analizar primeiro (), segundo **, terceiros * / **, quarto + - 
SIMPLIFICACAO: += vai pegar a propria variavel e somar com o numero "var n = 4" "n += 4" e assim da para fazer com os outros operdadores
RELACIONAIS(valor boolean verdadeiro ou falso):> pergunta se um numero e maior que ele , < pergunta se um numero e menor que ele,
>= pergunta se um numero e maior que ele ou igual ,<= pergunta se um numero e menor que ele ou igual,== pergunta se um numero e igual a ele,
!= pergunta se um numero e diferente dele
preco >= 200.50 o preco e maior ou igual a 200.50?
idade < 18 a idade e menor que 18?
curso == `JavaScrpit` o curso e igual a JavaScrpit?
n1 != n2 o numero n1 e diferentedo n2?
IDENTIDADE: === faz verificar se os dois sao do mesmo tipo de dado exemplo 5 === `5` false ou 5 >== 5 true
OPERADORES LOGICOS: ! negacao, && conjucao, || disjuncao
idade >= && <= 17 idade esta entre 15 e 17?
estado == `RJ` || estado == `SP`
salario > 1500 && sexo != `M`
&& → Conjunção (E)
- As duas condições precisam ser verdadeiras.
Exemplo:
idade >= 15 && idade <= 17
|| → Disjunção (OU)
- Pelo menos uma condição precisa ser verdadeira.
Exemplo:
estado == "RJ" || estado == "SP"
! → Negação (NÃO)
- Inverte o resultado da condição.
Exemplo:
!(idade >= 18)
RESUMO:
&& → E
|| → OU
!  → NÃO
OPERADORES TERNARIO: fuciona como se fosse uma pergunta 9>=10?`maior`:`menor` usa ? e :

DOM: Document Object Model, ele meio que fuciona no objetos do seu site, tem que saber a arvore do DOM
tem como selecionar o elemento por id, marca, classe, seletor e nome
INNER TEXT: E o texto dentro do elemento que vc tem, o innerText pega a formatacao sem os filhos que estao dentro do html. innerHTML pega tudo
 EXPLICANDO: window.document.getElementById(`id do elemento`), aqui voce esta seguindo um caminho para buscar um ID
 que se chama vermelho, voce consehue alterar varias coisas nele exemplo vermelho.style.color = `red` tambem da para colocar no final
 window.document.getElementById(`id do elemento`).innerText, mas somente se eu for alterar uma coisa no elemento
 
 DOM EVENTOS: Existe varios tipos de eventos diferentes, desde para celular e computador
 FUNCAO: E um conjuto de linhas que vao se executada so qaundo o evento ocorrer, essas limhas se cjamamam bloco, e sao determinadar dentro do {},
 voce tem que nomear ele com uma fuction no comeco dele, e depois nomear, tambem precisa colocar paranmetros dentro dela, ficando assim
 fuction evento(){

 }
FUNCAO CLICK: Para chamar a funcao de click se usa no html onclik = "nome()", onmouseenter chama a funcao de quando passa o mouse em cima
 onmouseout quando o mouse sair do elemento
 exemplo de codigo: 
 <div id="meio">
    <h2 id="vermelho">Inicio de manipulacao de dom</h2>
    <button onclick="evento()">Clique aqui</button>
  </div>

  function evento(){ //quando clicar o elemento chamado vermelho vai mudar o texto para clicou e logo depois fica verde
    var vermelho = window.document.getElementById(`vermelho`)
    vermelho.innerHTML = `Clicou`
    vermelho.style.color = `green`
}